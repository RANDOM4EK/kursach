import * as React from 'react';
import IconButton from '@mui/material/IconButton';

const Social = () => {
  return (
    <div>
      <IconButton>{/* Ваш код */}</IconButton>
    </div>
  );
};

export default Social;
